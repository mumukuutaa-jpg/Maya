[project]
name = "maya"
version = "1.0.0"
requires-python = ">=3.10"
dependencies = [
    "flet",
    "google-genai",
    "SpeechRecognition",
    "pyttsx3",
    "pyaudio",
    "requests"
]

[tool.flet.app]
name = "MayaAI"
product = "Maya Assistant"
org = "com.maya.assistant"
